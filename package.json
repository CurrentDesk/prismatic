{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "script": {
    "build": "lerna exec --bail=false -- yarn build",
    "deploy": "lerna publish --access public"
  },
  "devDependencies": {
    "lerna": "^2.10.2"
  },
  "resolutions": {
    "@types/graphql": "0.13.1",
    "graphql": "0.13.2",
		"mongodb": "3.1.0-beta4"
  }
}
